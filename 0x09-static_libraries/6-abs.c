#include "main.h"
/**
 * print_alphabet print alpha from a -> z
 * Return: void value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return(n);
	}
	if(n == 0)
	{
		return(0);
	}	
	if (n < 0)
	{
		return(-1 * n );
	}
	return(0);

}
