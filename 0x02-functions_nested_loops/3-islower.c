#include "main.h"
/**
 * print_alphabet print alpha from a -> z
 * Return: void value
 */
int print_alphabet(char c)
{	

	for (l = 'a'; l <= 'z'; l++)
	{
	if (c == l)
	{
		return(1);
	}	
	
	}
	return(0);

}
